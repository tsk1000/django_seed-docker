{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "django_seed-docker: debug",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/manage.py",
            "args": [
                "runserver",
                "--noreload",
                "8080" // 任意の値を入れる。
            ],
            "django": true
        }
    ]
}